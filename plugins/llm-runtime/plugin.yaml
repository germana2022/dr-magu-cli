id: llm-runtime
name: LLM Runtime
version: 1.1.4
enabled: true
domain: llm
description: Default-model LLM runtime with sanitized user-facing chat responses.

provides:
  agents:
    - llm-chat
  workflows:
    - llm.chat
  tools:
    - llm.chat
  prompts:
    - general-chat
    - intent-response
  schedules: []
  templates:
    - llm-response
permissions:
  network_outbound: restricted
