tools:
  website.build:
    description: Generate website proposal, architecture options, HITL request and report.
    category: website-builder
    read_only: false
    risk_level: high
    approval_required: false
    background_allowed: false
