agents:
  repository-analyzer:
    name: Repository Analyzer
    description: Analyze repository structure and generate deterministic project context through the repository.context workflow.
    role: repository_analysis
    enabled: true
    requires_llm: false
    workflow: repository.context
    aliases:
      - repo-analyzer
      - ra
    capabilities:
      - scan_repository
      - generate_context
      - summarize_project_structure
    model:
      provider: null
      base_url: null
      model: null
      temperature: null
